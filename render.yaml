services:
  - type: web
    name: ultar-md-bot 
    env: node
    plan: free
    buildCommand: "npm install" # <-- This line fixes the error
    startCommand: "node index.js"
    # ... rest of your file ...
